##############################################
# OnePage-Section: neuer Seitentyp (doktype 201)
##############################################

mod.wizards.newPage.wizardItems.onepage {
    header = OnePage
    elements.onepage_section {
        iconIdentifier = onepage-section
        title = OnePage-Section
        description = Abschnitt für die OnePage-Ausgabe
        tt_content_defValues {
            doktype = 201
        }
    }
    show := addToList(onepage_section)
}
